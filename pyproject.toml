[project]
name = "sucks"
dynamic = ["version"]
dependencies = [
    "PyYAML",
    "pydantic"
]


[project.scripts]
sucks="sucks.main:main"

[tool.setuptools.dynamic]
version = {attr = "sucks.__version__"}
readme = {file=["README.md"]}